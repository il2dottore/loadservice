services:
  attack-node-router:
    image: ${ECR_REGISTRY:?Set ECR_REGISTRY}/loadservice-attack-node-router:latest
    env_file:
      - ./attack-node-router/.env
    restart: unless-stopped

  api-gateway:
    image: ${ECR_REGISTRY:?Set ECR_REGISTRY}/loadservice-api-gateway:latest
    env_file:
      - ./api-gateway/.env
    volumes:
      - ./api-gateway/config.json:/app/config.json:ro
    ports:
      - "8080:8080"
    restart: unless-stopped

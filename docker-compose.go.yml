services:
  attack-node-router:
    image: sussybakacute/loadservice-attack-node-router:latest
    env_file:
      - ./attack-node-router/.env
    restart: unless-stopped

  api-gateway:
    image: sussybakacute/loadservice-api-gateway:latest
    env_file:
      - ./api-gateway/.env
    volumes:
      - ./api-gateway/config.json:/app/config.json:ro
    ports:
      - "8080:8080"
    restart: unless-stopped
